import 'package:flutter/material.dart';

class ConvertScreen extends StatelessWidget {
  static const String routeName = 'ConvertScreen';
  const ConvertScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
